import logging.config  # noqa: F401

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '<your SECRET_KEY generated in settings.py from startproject command would go here>'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# Set CronTab user for recurring cronjobs
CRON_USER = 'www-data'

ALLOWED_HOSTS = ['localhost','127.0.0.1','servername','servername.company.com']

# Email backend
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'

# SMTP server info
EMAIL_HOST = 'smtp.yourcompany.com'
EMAIL_PORT = 587
EMAIL_HOST_USER = 'your-username@yourcompany.com'
EMAIL_HOST_PASSWORD = 'yourpassword'
EMAIL_USE_TLS = True   # or EMAIL_USE_SSL = True if using port 465

# Default "From" address for outgoing mail
DEFAULT_FROM_EMAIL = 'Network Ops Dashboard <noreply@yourcompany.com>'

LOGGING = {
    'version': 1,
    'disable_existing_loggers': True,
    'formatters': {
        'standard': {
            'format': '%(asctime)s [%(levelname)s] %(name)s: %(message)s'
        },
    },
    'handlers': {
        'default': {
            'level':'DEBUG',
            'class':'logging.handlers.RotatingFileHandler',
            'filename': '/pathto/vproject_venvenv/logs/debug.log',
            'maxBytes': 1024*1024*5, # 5 MB
            'backupCount': 5,
            'formatter':'standard',
        },  
        'request_handler': {
            'level':'DEBUG',
            'class':'logging.handlers.RotatingFileHandler',
            'filename': '/pathto/project_venv/logs/request_debug.log',
            'maxBytes': 1024*1024*5, # 5 MB
            'backupCount': 5,
            'formatter':'standard',
        },
    },
    'loggers': {
        '': {
            'handlers': ['default'],
            'level': 'DEBUG',
            'propagate': True
        },
        'django.request': {
            'handlers': ['request_handler'],
            'level': 'DEBUG',
            'propagate': False
        },
    }
}